Этот репозиторий = плагин Shipping Game (тип: плагин; remote CaptTymur/skipi-shipping-game).

# AGENTS.md — плагин (минимальная строгость, жёсткие границы)

Плагин — потребитель API plugin-host. Радиус поражения минимальный, поводок длинный, но границы жёсткие.

## Правила плагина

- **Хост НЕ менять.** plugin-host и его API — чужая территория. Не хватает API — НЕ расширяй хост сам: handoff в штаб (`skipi-ops/handoffs/`), решает человек.
- **plugin.json свят:** поля не удалять и не переименовывать. Добавлять новые — можно, осознанно.
- **checksums.json обновляется при любом изменении файлов плагина** — рассинхрон = красный bundled-loader.
- Достаточно собственных тестов плагина; в verify_matrix плагин НЕ shared.
- **Публикацию remote-версии выполняет только человек вручную.** Ты готовишь код и тесты; pack/signing/выкладку не запускаешь.

## Общий протокол (одинаков во всех репо Skipi)

- Одна задача = один worktree = одна сессия. Задача закрыта — сессия закрывается; новая задача = свежая сессия.
- Минимальный дифф: меняй только то, что требует задача.
- Bugfix = сначала failing-test, воспроизводящий баг → фикс → зелёный прогон. Фикс без failing-test'а не принимается.
- После работы: commit + push.
- `--no-verify` запрещён.
- systemd, деплой, прод-сервер и флот ботов — вне скоупа; нужно — handoff в штаб (`skipi-ops/handoffs/`).
- Инцидент (регрессия, потеря данных) → SKI-INC-NNN в `skipi-ops/incidents/`.
- Сессия запускается из папки этого репо (`cd <репо> && claude`), не из корня. Если этого файла нет в списке `/memory` — сессия поднята неправильно: остановись и сообщи человеку.
- Обмен с другими агентами — через артефакты на диске (коммиты, handoffs, verify-отчёты), не копипастом между окнами.
